# Metadata about this component, to be used in the CMS
spec:
  structures:
    - content_blocks
    - extra_blocks
  label: "Pricing Cards"
  description: Show your pricing through cards!
  icon:
  tags: []
  tabbed: true

# Defines the structure of this component, as well as the default values
blueprint:
  _componentId: pricingCardsSection-ad4a1a49-2213-46ca-9cc2-271fa7e16efa
  content:
    sectionId:
    heading: bookshop:structure:headingBlocks
    cards: [bookshop:generic/pricingCard!]
    buttons: [bookshop:structure:buttons]
  styles:
    color_group: primary

# Overrides any fields in the blueprint when viewing this component in the component browser
preview:

# Any extra CloudCannon inputs configuration to apply to the blueprint
_inputs:
